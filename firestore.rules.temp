rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rules for initialization - ALLOW ALL ACCESS
    match /{document=**} {
      allow read, write: if true;
    }
  }
}