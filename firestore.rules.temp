rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rule for initialization - allows all reads and writes
    match /{document=**} {
      allow read, write: if true;
    }
  }
}